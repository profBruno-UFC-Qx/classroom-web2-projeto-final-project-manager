From f24555c7f5e19faab31133f0c934fcf2e8f29898 Mon Sep 17 00:00:00 2001
From: fernandocastro <fernandopaulino00@gmail.com>
Date: Fri, 2 Jan 2026 11:11:57 -0300
Subject: [PATCH] Cria/atualiza telas Criar de sprint, tarefa e comentario

---
 frontend/src/pages/Sprint/Criar.vue | 58 +++++++++++++++++++++++++++--
 frontend/src/pages/Task/Criar.vue   | 57 ++++++++++++++++++++++++++--
 2 files changed, 109 insertions(+), 6 deletions(-)

diff --git a/frontend/src/pages/Sprint/Criar.vue b/frontend/src/pages/Sprint/Criar.vue
index b01e2ce..940269f 100644
--- a/frontend/src/pages/Sprint/Criar.vue
+++ b/frontend/src/pages/Sprint/Criar.vue
@@ -1,5 +1,57 @@
-<script setup></script>
+<script setup>
+import { ref } from "vue";
+import { useRouter } from "vue-router";
+import api from "../../services/axios.js";
+import SprintForm from "../../components/SprintForm.vue";
+import Container from "../../components/Container.vue";
+
+const props = defineProps({
+  user: { type: Object, required: true },
+  projectId: { type: [String, Number], required: true },
+});
+
+const router = useRouter();
+const errors = ref({});
+
+function goBack() {
+  router.push(`/sprint/listar/${props.projectId}`);
+}
+
+async function handleSubmit(payload) {
+  errors.value = {};
+
+  try {
+    // garante que o sprint vai associado ao projeto
+    const body = {
+      ...payload,
+      projectId: Number(props.projectId),
+      ownerId: 1, // se seu form já manda ownerId, pode remover daqui
+    };
+
+    await api.post("/sprints", body);
+    router.push(`/sprint/listar/${props.projectId}`);
+  } catch (error) {
+    if (error.response && error.response.status === 422) {
+      errors.value = error.response.data.errors;
+    } else {
+      console.error(error);
+    }
+  }
+}
+</script>
 
 <template>
-    <h1>Criar</h1>
-</template>
\ No newline at end of file
+  <Container>
+    <div class="w-full mb-6 flex justify-between">
+      <h1 class="text-4xl font-bold text-gray-800">Criar Sprint</h1>
+      <button
+        @click="goBack"
+        class="px-4 py-2 bg-gray-800 text-white rounded-lg hover:bg-gray-700 transition"
+      >
+        ← Voltar
+      </button>
+    </div>
+
+    <SprintForm @submit="handleSubmit" :errors="errors" :ownerId="1" :projectId="Number(projectId)" />
+  </Container>
+</template>
diff --git a/frontend/src/pages/Task/Criar.vue b/frontend/src/pages/Task/Criar.vue
index c036a45..8b27f16 100644
--- a/frontend/src/pages/Task/Criar.vue
+++ b/frontend/src/pages/Task/Criar.vue
@@ -1,5 +1,56 @@
-<script setup></script>
+<script setup>
+import { ref } from "vue";
+import { useRouter } from "vue-router";
+import api from "../../services/axios.js";
+import TaskForm from "../../components/TaskForm.vue";
+import Container from "../../components/Container.vue";
+
+const props = defineProps({
+  user: { type: Object, required: true },
+  sprintId: { type: [String, Number], required: true },
+});
+
+const router = useRouter();
+const errors = ref({});
+
+function goBack() {
+  router.push(`/tarefa/listar/${props.sprintId}`);
+}
+
+async function handleSubmit(payload) {
+  errors.value = {};
+
+  try {
+    const body = {
+      ...payload,
+      sprintId: Number(props.sprintId),
+      ownerId: 1,
+    };
+
+    await api.post("/tasks", body);
+    router.push(`/tarefa/listar/${props.sprintId}`);
+  } catch (error) {
+    if (error.response && error.response.status === 422) {
+      errors.value = error.response.data.errors;
+    } else {
+      console.error(error);
+    }
+  }
+}
+</script>
 
 <template>
-    <p>Criar Tarefa</p>
-</template>
\ No newline at end of file
+  <Container>
+    <div class="w-full mb-6 flex justify-between">
+      <h1 class="text-4xl font-bold text-gray-800">Criar Tarefa</h1>
+      <button
+        @click="goBack"
+        class="px-4 py-2 bg-gray-800 text-white rounded-lg hover:bg-gray-700 transition"
+      >
+        ← Voltar
+      </button>
+    </div>
+
+    <TaskForm @submit="handleSubmit" :errors="errors" :ownerId="1" :sprintId="Number(sprintId)" />
+  </Container>
+</template>
-- 
2.50.1.windows.1

